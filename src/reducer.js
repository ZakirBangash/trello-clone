const reducer = (state, action) => {
    
        switch (action.type) {
          case "addUser": {
            console.log("AddUser of the Sign")
                return state;
          }
          
          default:
            return state;
        }
      };
  
      export default reducer;
  